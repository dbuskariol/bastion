import Foundation

/// Minimal canonical types that the rest of the codebase (and tests)
/// import via `BastionCore`. Filled out in commit 2 onwards as features
/// land; commit 1 just needs the module to compile.

public enum BastionCore {
    public static let schemaVersion = 1
}
