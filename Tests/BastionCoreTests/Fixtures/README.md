# Fixtures

Golden files and per-shell fixtures used by `BastionCoreTests`.

Populated incrementally per commit:
- Commit 3 adds SSH config writer golden files + scanner fixtures.
- Commit 6 adds per-shell history fixtures + extractor test inputs.
- Commit 7 adds AppleScript escape fuzz fixtures.
